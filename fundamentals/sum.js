/* eslint-disable no-unused-vars */
const assert = require('assert')

/**
 *
 * @param { number } num1
 * @param { number } num2
 * @returns { number } sum
 */
function add(num1, num2) {
  return num1 + num2;
}

module.exports = { add };
